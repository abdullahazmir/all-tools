NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Production only — server-only origin the Next.js rewrite proxies /api/* to,
# so client-side requests stay same-origin (needed for cross-domain auth cookies).
# API_ORIGIN=https://toolbazaar-api.onrender.com
